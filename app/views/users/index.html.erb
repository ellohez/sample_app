<%= provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
<!--    Rails infers that @users is a list of User objects -->
<!--    when called with a collection, Rails automatically iterates each -->
<!-- and looks for a partial called _user.html.erb-->
    <%= render @users %>
</ul>

<%= will_paginate %>
